<Window x:Class="VisionInspection.WndLogOn"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:VisionInspection"
        mc:Ignorable="d"
        ResizeMode="NoResize" WindowStartupLocation="CenterOwner"
        Title="Log On" Height="320" Width="480">
    <Grid>
        <Grid.Resources>
            <Style TargetType="Label">
                <Setter Property="VerticalContentAlignment" Value="Center"/>
                <Setter Property="HorizontalContentAlignment" Value="Center"/>
                <Setter Property="FontSize" Value="20"/>
                <Setter Property="FontWeight" Value="Bold"/>
                <Setter Property="Margin" Value="2,2,2,2"/>
            </Style>
            <Style TargetType="TextBox">
                <Setter Property="VerticalContentAlignment" Value="Center"/>
                <Setter Property="HorizontalContentAlignment" Value="Left"/>
                <Setter Property="FontSize" Value="20"/>
                <Setter Property="FontWeight" Value="Bold"/>
                <Setter Property="Margin" Value="2,2,2,2"/>
            </Style>
            <Style TargetType="Button" BasedOn="{StaticResource mButton}">
                <Setter Property="VerticalContentAlignment" Value="Center"/>
                <Setter Property="HorizontalContentAlignment" Value="Center"/>
                <Setter Property="FontSize" Value="20"/>
                <Setter Property="FontWeight" Value="Bold"/>
                <Setter Property="Margin" Value="2,2,2,2"/>
            </Style>
        </Grid.Resources>
        <Grid.RowDefinitions>
            <RowDefinition Height="1.5*"/>
            <RowDefinition Height="1*"/>
            <RowDefinition Height="1*"/>
            <RowDefinition Height="1.5*"/>
            <RowDefinition Height="1.5*"/>
            <RowDefinition Height="0.1*"/>
        </Grid.RowDefinitions>

        <Label Grid.Row="0" Background="DeepSkyBlue">Enter ID and Password </Label>
        
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1*"/>
                <ColumnDefinition Width="2*"/>
            </Grid.ColumnDefinitions>
            
            <Label Grid.Column="0" Background="DarkGray" Foreground="White">ID</Label>
            <ComboBox Grid.Row="1" Grid.Column="1"  x:Name="cboUsername" SelectedValue="Operator" FontSize="20" VerticalContentAlignment="Center" HorizontalContentAlignment="Center" SelectedValuePath="Content" Margin="2,2,2,2">
            <ComboBoxItem Tag="Super">SuperUser</ComboBoxItem>
            <ComboBoxItem Tag="Manager">Manager</ComboBoxItem>
            <ComboBoxItem Tag="Operator">Operator</ComboBoxItem>
            </ComboBox>
        </Grid>
        
        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1*"/>
                <ColumnDefinition Width="2*"/>
            </Grid.ColumnDefinitions>

            <Label Grid.Column="0" Background="DarkGray" Foreground="White">Password</Label>
            <PasswordBox Grid.Column="1" x:Name="txtPassword" TabIndex="0"
                         Margin="2,2,2,2" VerticalContentAlignment="Center" HorizontalContentAlignment="Left"></PasswordBox>
        </Grid>

        <UniformGrid Grid.Row="3" Columns="2">
            <Button x:Name="btOk" Foreground="DodgerBlue">OK</Button>
            <Button x:Name="btCancel" Foreground="OrangeRed">Cancel</Button>
        </UniformGrid>

        <Button Grid.Row="4" x:Name="btChangePassword" Foreground="DarkViolet">Change Password</Button>
    </Grid>
</Window>
