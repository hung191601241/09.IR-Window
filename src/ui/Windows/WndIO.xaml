<Window x:Class="VisionInspection.WndIO"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:VisionInspection"
        mc:Ignorable="d"
        WindowStartupLocation="CenterOwner" ShowInTaskbar="False" 
        WindowStyle="None" WindowState="Maximized"
        Title="WndIO" Height="900" Width="1300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="60"/>
            <RowDefinition />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Background="#444444">
            <Grid.Resources>
                <Style TargetType="{x:Type Label}">
                    <Setter Property="Foreground" Value="White" />
                    <Setter Property="Background" Value="#879ec8" />
                    <Setter Property="BorderThickness" Value="0" />
                    <Setter Property="Padding" Value="5,0" />
                    <Setter Property="VerticalContentAlignment" Value="Center" />
                    <Setter Property="HorizontalContentAlignment" Value="Center" />
                </Style>
                <Style TargetType="{x:Type Button}">
                    <Setter Property="Foreground" Value="White" />
                </Style>
                <Style TargetType="{x:Type Grid}">
                    <Setter Property="Margin" Value="0,0,0,0" />
                </Style>
            </Grid.Resources>

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="3*"></ColumnDefinition>
                <ColumnDefinition Width="4*"></ColumnDefinition>
                <ColumnDefinition Width="2*"></ColumnDefinition>
                <ColumnDefinition Width="3*"></ColumnDefinition>
                <ColumnDefinition Width="2*"></ColumnDefinition>
                <ColumnDefinition Width="2*"></ColumnDefinition>
                <ColumnDefinition Width="2*"></ColumnDefinition>
                <ColumnDefinition Width="2*"></ColumnDefinition>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="60"></RowDefinition>
            </Grid.RowDefinitions>

            <Image Grid.Row="0" Grid.Column="0" Source="Images/logo.jpg" Stretch="Fill"></Image>

            <Grid Grid.Row="0" Grid.Column="1">
                <Grid.RowDefinitions>
                    <RowDefinition Height="1*"></RowDefinition>
                    <RowDefinition Height="1*"></RowDefinition>
                </Grid.RowDefinitions>
                <Label Grid.Row="0" FontSize="20" FontWeight="Bold">20CH Multi Tester</Label>
                <Label Grid.Row="1" Name="lblVersion" Background="#6787c0">Ver 1.0.0 (2020.06.04)</Label>
            </Grid>

            <Grid Grid.Row="0" Grid.Column="2">
                <Grid.RowDefinitions>
                    <RowDefinition Height="1*"></RowDefinition>
                    <RowDefinition Height="1*"></RowDefinition>
                </Grid.RowDefinitions>
                <Label Grid.Row="0" FontSize="20" Background="#b8b7bf"
                       Name="lblCurrentDate"></Label>
                <Label Grid.Row="1" Name="lblCurrentTime" FontSize="16"></Label>
            </Grid>

            <Grid Grid.Row="0" Grid.Column="3">
                <Button Style="{StaticResource ButtonMain50}" Name="btMain">
                    <StackPanel>
                        <Label Grid.Row="0" FontSize="20" FontWeight="Medium" Background="Transparent" Margin="0,12,0,0">MAIN</Label>
                        <Label Grid.Row="1" Background="Transparent" Margin="0,-2,0,0">Main Page</Label>
                    </StackPanel>
                </Button>
            </Grid>

            <Grid Grid.Row="0" Grid.Column="4">
                <Button Style="{StaticResource ButtonMain50}" Name="btMenu">
                    <StackPanel>
                        <Label Grid.Row="0" FontSize="20" FontWeight="Medium" 
                                Background="Transparent" Margin="0,12,0,0">MENU</Label>
                        <Label Grid.Row="1" Background="Transparent" Margin="0,-2,0,0">Main Menu</Label>
                    </StackPanel>
                </Button>
            </Grid>

            <Grid Grid.Row="0" Grid.Column="5" Background="#6787c0">
                <Button Style="{StaticResource ButtonMain50}" Name="btIO" Background="SkyBlue">
                    <StackPanel>
                        <Label Grid.Row="0" FontSize="20" FontWeight="Medium" 
                                Background="Transparent" Margin="0,12,0,0">I/O</Label>
                        <Label Grid.Row="1" Background="Transparent" Margin="0,-2,0,0">Input/Output</Label>
                    </StackPanel>
                </Button>
            </Grid>

            <Grid Grid.Row="0" Grid.Column="6" Margin="0.5,0,0,0">
                <Button Style="{StaticResource ButtonMain50}" Name="btLastJam">
                    <StackPanel>
                        <Label Grid.Row="0" FontSize="20" FontWeight="Medium" 
                                Background="Transparent" Margin="0,12,0,0">LAST JAM</Label>
                        <Label Grid.Row="1" Background="Transparent" Margin="0,-2,0,0">Last jam display</Label>
                    </StackPanel>
                </Button>
            </Grid>

            <Grid Grid.Row="0" Grid.Column="7" Margin="0,0,0,0">
                <Button Name="btPower" Style="{StaticResource mButton}">
                    <Button.Content>
                        <Image Source="Images/power-button.png"></Image>
                    </Button.Content>
                </Button>
            </Grid>
        </Grid>

        <Grid Grid.Row="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="1*"/>
                <RowDefinition Height="6.5*"/>
                <RowDefinition Height="1.5*"/>
            </Grid.RowDefinitions>
            <Border Grid.Row="0" BorderBrush="#bbbbbb" BorderThickness="2">
                <Label Name="lblTitle" FontSize="28" FontWeight="Bold" Foreground="DodgerBlue"
                   VerticalContentAlignment="Center">MODULE TYPE: INPUT 32</Label>
            </Border>

            <UniformGrid Grid.Row="1" Columns="2">
                <UniformGrid.Resources>
                    <Style TargetType="Label">
                        <Setter Property="BorderBrush" Value="#bbbbbb"/>
                        <Setter Property="BorderThickness" Value="1"/>
                        <Setter Property="FontSize" Value="20"/>
                        <Setter Property="FontWeight" Value="Bold"/>
                        <Setter Property="HorizontalContentAlignment" Value="Center"/>
                        <Setter Property="VerticalContentAlignment" Value="Center"/>
                        <Setter Property="Padding" Value="2,0,2,0"/>
                    </Style>
                </UniformGrid.Resources>
                <Grid x:Name="gridIO0015">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="1*"/>
                        <ColumnDefinition Width="3*"/>
                        <ColumnDefinition Width="1*"/>
                    </Grid.ColumnDefinitions>
                </Grid>
                <Grid x:Name="gridIO1631">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="1*"/>
                        <ColumnDefinition Width="3*"/>
                        <ColumnDefinition Width="1*"/>
                    </Grid.ColumnDefinitions>
                </Grid>
            </UniformGrid>

            <Grid Grid.Row="2">
                <Grid.Resources>
                    <Style TargetType="Button" BasedOn="{StaticResource mButton}">
                        <Setter Property="Margin" Value="10,10,10,10"/>
                    </Style>
                    <Style TargetType="Rectangle">
                        <Setter Property="Width" Value="100"/>
                        <Setter Property="Height" Value="10"/>
                        <Setter Property="Fill" Value="Silver"/>
                        <Setter Property="StrokeThickness" Value="2"/>
                        <Setter Property="Stroke" Value="Gray"/>
                    </Style>
                    <Style TargetType="TextBlock">
                        <Setter Property="HorizontalAlignment" Value="Center"/>
                        <Setter Property="VerticalAlignment" Value="Center"/>
                        <Setter Property="FontSize" Value="24"/>
                        <Setter Property="FontWeight" Value="Bold"/>
                    </Style>
                </Grid.Resources>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="3*"/>
                    <ColumnDefinition Width="1*"/>
                    <ColumnDefinition Width="1*"/>
                </Grid.ColumnDefinitions>

                <Button Grid.Column="1" Name="btInput">
                    <StackPanel>
                        <Rectangle Name="rectInput"/>
                        <TextBlock>Input I/O</TextBlock>
                    </StackPanel>
                </Button>
                <Button Grid.Column="2" Name="btOutput">
                    <StackPanel>
                        <Rectangle Name="rectOutput"/>
                        <TextBlock>Output I/O</TextBlock>
                    </StackPanel>
                </Button>
            </Grid>
        </Grid>
    </Grid>
</Window>
